import React from "react"
import { Footer, FooterContainer } from "./footer.elements"

export default () => {
  return (
    <Footer>
      <FooterContainer>footer</FooterContainer>
    </Footer>
  )
}
